#include "main.h"
/**
 * mul - find the product of 2 integers
 *
 * @a: first number to multiply
 * @b: second number to multiply
 *
 * Returns: the product
 */
int mul(int a, int b)
{
	return (a * b);
}
