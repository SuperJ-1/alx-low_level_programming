#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: prints all single digits in base 10
 *
 * Return: returns 0
 */
int main(void)
{
	char sdinb10 = 0;

	while (sdinb10 <= 10)
		;
	{
		putchar(sdinb10);
		sdinb10++;
	}
	putchar('\n');
	return (0);
}
